// vite.config.js
import { defineConfig } from "vite";

export default defineConfig({
	base: "/wordle/", // Example: if your app is served from example.com/my-app/
	// Other Vite configuration options...
});
